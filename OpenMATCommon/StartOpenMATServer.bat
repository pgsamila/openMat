@echo off
@echo [OpenMAT] Starting OpenMAT network server
@echo [OpenMAT] Publisher and subscriber applications can now connect
@echo [OpenMAT] Do not close this command prompt window, unless you want to quit the server
icebox --Ice.Config=config.icebox
